jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.2-open
  - sdk use java 17.0.2-open
env:
  JITPACK: true
install:
  - echo "Running custom install command"
  - ./gradlew clean build publishToMavenLocal -x test -x signMavenPublication -x signPatch-corePublication -x signUpdatePublication -x signPatch-generator-androidPublication
